{
  "version": 2,
  "buildCommand": "[ -d frontend ] && npm run build --prefix frontend || npm run build",
  "installCommand": "[ -d frontend ] && npm install --prefix frontend || npm install",
  "outputDirectory": "[ -d frontend ] && frontend/.next || .next",
  "framework": "nextjs"
}
